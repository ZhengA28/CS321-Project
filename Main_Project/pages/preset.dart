import 'package:flutter/material.dart';

class PresetPage extends StatelessWidget {
  const PresetPage({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text("Preset Exercise")),
    );
  }
}